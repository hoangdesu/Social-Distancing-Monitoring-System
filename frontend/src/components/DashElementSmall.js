import React from 'react';

const DashElementSmall = (props) => {
    return (
        <div className={props.styling}>
            Small element
        </div>
    );
};

export default DashElementSmall;
